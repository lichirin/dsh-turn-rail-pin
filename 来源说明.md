# 来源

本机这份 `dsh-turn-rail-pin` 没有指向外部作者的仓库地址。`package.json` 声明许可证为 MIT，目录里原来没有单独的 `LICENSE` 文件。

- 本地版本号：`0.1.7-local.1`
- 接到 DeepSeek Harness `0.1.7-rc.1` 的改动：去掉 `settings.register` 和 `settingsScope`，开关改写在插件自己的 `Config` 里，浏览器通过 `configForms` 读写

本机账户没有被写成作者。
